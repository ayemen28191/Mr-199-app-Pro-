ممتاز، إذًا نحتاج الآن إلى تقرير تصفية جماعية لجميع العمال المرتبطين بمشروع محدد، بحيث يظهر في التقرير:

إجمالي استحقاقات كل عامل (حسب ساعات/أيام العمل)

مجموع السلف

مجموع المدفوعات

الاستقطاعات (إن وُجدت)

الرصيد النهائي لكل عامل

ثم ملخص إجمالي في نهاية التقرير لكل الأعمدة (استحقاقات، سلف، مدفوعات... إلخ)



---

✅ هيكل التقرير الجماعي – تصميم احترافي

🎯 الغرض:

حصر مستحقات كافة العمال في مشروع محدد (أو فترة محددة) بطريقة مجمعة تسهّل المحاسبة أو التصفية النهائية لهم.


---

🧱 الجداول المطلوبة (نفس السابق)

الجدول	الغرض الرئيسي

workers	بيانات العمال
work_sessions	جلسات/أيام العمل والمبالغ المكتسبة
advances	السلف
payments	الدفعات المدفوعة
deductions	(اختياري) استقطاعات أخرى كالغرامات أو المديونية



---

📊 نتيجة الاستعلام النهائي (تقرير جماعي):

#	اسم العامل	إجمالي العمل	الاستحقاق	السلف	المدفوعات	الاستقطاعات	الرصيد النهائي

1	محمد صالح	120 ساعة	1,800 ر.ي	500	1,200	50	50
2	خالد سعيد	100 ساعة	1,500 ر.ي	200	1,000	0	300
…	…	…	…	…	…	…	…
	الإجمالي	220 ساعة	3,300 ر.ي	700	2,200	50	350



---

📥 استعلام SQL لإنشاء التقرير

نفترض الدفع بالساعة، ويمكن تعديل الاستعلام ليحسب بالأيام أو الأجر الثابت:

SELECT
  w.worker_id,
  w.name AS worker_name,
  SUM(ws.hours_worked) AS total_hours,
  ROUND(AVG(ws.hourly_rate), 2) AS avg_hourly_rate,
  SUM(ws.hours_worked * ws.hourly_rate) AS gross_amount,
  COALESCE(SUM(a.amount), 0) AS total_advances,
  COALESCE(SUM(p.amount), 0) AS total_payments,
  COALESCE(SUM(d.amount), 0) AS total_deductions,
  (SUM(ws.hours_worked * ws.hourly_rate)
   - COALESCE(SUM(a.amount), 0)
   - COALESCE(SUM(p.amount), 0)
   - COALESCE(SUM(d.amount), 0)
  ) AS final_balance
FROM workers w
LEFT JOIN work_sessions ws
  ON ws.worker_id = w.worker_id AND ws.project_id = :project_id
LEFT JOIN advances a
  ON a.worker_id = w.worker_id AND a.project_id = :project_id
LEFT JOIN payments p
  ON p.worker_id = w.worker_id AND p.project_id = :project_id
LEFT JOIN deductions d
  ON d.worker_id = w.worker_id AND d.project_id = :project_id
WHERE w.worker_id IN (
  SELECT DISTINCT worker_id FROM work_sessions WHERE project_id = :project_id
)
GROUP BY w.worker_id, w.name
ORDER BY w.name;

> ❗ استبدل :project_id بمعرّف المشروع المطلوب.




---

🧾 تنسيق التقرير (PDF / Excel):

🧩 رأس التقرير:

شعار المشروع

اسم المشروع

اسم الجهة/المحاسب

التاريخ

الفترة المغطاة


📄 جسم التقرير: جدول العمال (كما في المثال أعلاه)

🧮 ذيل التقرير:

المجاميع العامة:

إجمالي الساعات لكل العمال

مجموع الاستحقاقات

مجموع السلف

مجموع المدفوعات

مجموع الاستقطاعات

إجمالي الرصيد النهائي




---

🔁 خيارات التصفية المقترحة

🔽 المشروع

🔽 الشهر/الفترة

🔽 العامل (اختياري)

📆 نطاق تواريخ



---

🖨️ التصدير والطباعة

[✅] PDF احترافي برأس وتذييل

[✅] Excel لتسهيل الإدخال في برامج أخرى

[✅] مشاركة التقرير كرابط أو ملف عبر البريد



---

🎁 ملاحظات احترافية

استخدم تنسيق العملة (ر.ي) لكل الأعمدة المالية

احسب متوسط الأجر بدقة لو تم تعديله خلال الفترة

أضف لونًا مميزًا للعمال الذين لهم رصيد سلبي (مديونية)

ضع ملحوظة (مكتملة التصفية / لم تُصفى بعد)



---

هل ترغب أن أكتب لك تقريرًا كاملًا بصيغة PDF أو Excel كقالب تجريبي لهذا التنسيق؟

