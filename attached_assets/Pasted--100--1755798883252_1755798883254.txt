تمام، الآن الصورة أوضح 100% ✅
يعني النظام المطلوب سيكون نظام إدارة أدوات + ربطه بالمشتريات والمصروفات بحيث:

1. عند شراء أداة جديدة في قسم المشتريات، يتم إدخالها تلقائيًا إلى سجل الأدوات.


2. يميز النظام بين:

أدوات مستهلكة/منتهية الصلاحية (مثلاً جلخ أو بطاريات أو أي أداة تنتهي سريعًا).

أدوات غير مستهلكة (دريلات، مطارق، مكينة لحام…).



3. كل أداة ترتبط بمشروع محدد أو المخزن الرئيسي عند إضافتها.




---

🔄 تعديل قاعدة البيانات لتدعم هذا السيناريو

جدول الأدوات (tools)

CREATE TABLE tools (
  id VARCHAR PRIMARY KEY DEFAULT gen_random_uuid(),
  code TEXT UNIQUE,                  -- كود أو باركود الأداة
  name TEXT NOT NULL,
  type TEXT,                         -- نوع الأداة (كهربائي/يدوي/مستهلك)
  is_consumable BOOLEAN DEFAULT FALSE, -- هل الأداة مستهلكة (جلخ، بطارية…)
  status TEXT DEFAULT 'available',   -- متاحة/مستخدمة/معطلة/منتهية
  current_project_id VARCHAR REFERENCES projects(id), 
  purchase_id VARCHAR REFERENCES purchases(id), -- ربط بالمشتريات
  notes TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

جدول المشتريات (purchases)

CREATE TABLE purchases (
  id VARCHAR PRIMARY KEY DEFAULT gen_random_uuid(),
  item_name TEXT NOT NULL,
  quantity INT NOT NULL,
  purchase_type TEXT,     -- نقدي / آجل
  supplier TEXT,
  purchase_date TIMESTAMP DEFAULT NOW(),
  project_id VARCHAR REFERENCES projects(id),  -- إذا كانت الأداة لمشروع معين
  total_cost DECIMAL(12,2),
  notes TEXT
);


---

⚡ تدفق العمل الجديد

1. شراء أداة جديدة:

قسم المشتريات يسجل العملية في جدول purchases.

عند التسجيل، إذا كانت أداة فعلية وليست مستهلكة، يتم إضافة سجل تلقائي في جدول tools.

إذا كانت أداة مستهلكة (جلخ، أسلاك، بطاريات)، يمكن تسجيلها كمصروف وتحديث المخزون فقط.



2. تمييز المستهلك من غير المستهلك:

is_consumable = TRUE → استهلاكية.

is_consumable = FALSE → غير مستهلكة، تُنقل بين المشاريع أو المخازن.



3. التقارير المعدلة:

كشف أدوات المشروع مع حالة الاستهلاك.

كشف مشتريات الأدوات الجديدة مع رابطها بسجل الأدوات.

سجل حركة الأداة (من المخزن إلى المشروع).





---

إذا أحببت، أقدر أرسم لك مخطط تدفق العمليات (من المشتريات → إضافة أداة → المشروع → كشف الأدوات) بحيث يكون واضح كل شيء من لحظة الشراء إلى استخدام الأداة.

هل تريد أن أفعل ذلك الآن؟

