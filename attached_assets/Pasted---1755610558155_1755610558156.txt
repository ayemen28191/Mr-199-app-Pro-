ممتاز يا عمار — خلّيت لك خطة عمل واضحة، خطوة بخطوة، مع أوامر جاهزة تنفّذها الآن. سأقسمها حسب الأولوية (فوري — قصير المدى — متوسط/طويل المدى)، وأرفق سكربتات/أوامر وملف CI جاهز للصق. لنبدأ.

أولاً — خطوات فورية (نفّذها الآن — 30–90 دقيقة)

1. أنشئ فرع عمل لحماية الرئيسي:



git checkout -b audit/cleanup-$(date +%F)

2. تأكد أن البناء والاختبارات نظيفة:



# TypeScript build check
npx tsc --noEmit

# build (إذا عندك سكربت build)
npm run build || pnpm build || yarn build

# ESLint (تأكيد أن الإعداد الجديد يعمل)
npx eslint . --ext .js,.jsx,.ts,.tsx || true

# اختبارات الوحدة
npm test || pnpm test || yarn test || true

إذا أي أمر رجع خطأ — علّق الأخطاء (نسخ/لصق) واصلح أولاً قبل حذف أي شيء.

3. توليد قائمة بالحزم المحتملة غير المستخدمة (من التقرير نملك قائمة مقترحة) — شغّل depcheck واحفظ النتائج:



npx depcheck --json > audit-results/depcheck.json
jq . audit-results/depcheck.json

4. فحص كل حزمة مُقترحة يدويًا بالبحث عن أي استيراد أو استدعاء ديناميكي:



# مثال: تحقق من وجود استدعاءات للحزمة 'framer-motion'
rg -n "from ['\"]framer-motion['\"]|require\(['\"]framer-motion['\"]\)" || echo "لا توجد إشارات استيراد صريحة لـ framer-motion"

# سكربت سريع للتحقق لكل حزمة مذكورة في التقرير
for pkg in @jridgewell/trace-mapping connect-pg-simple framer-motion memorystore next-themes openid-client passport passport-local react-icons tw-animate-css xlsx; do
  echo "=== $pkg ==="
  rg -n "from ['\"]$pkg['\"]|require\(['\"]$pkg['\"]\)" || rg -n "$pkg" || echo "لم يُعثر على إشارات مباشرة — افحص يدويًا"
done

ملاحظة مهمة: بعض الحزم تُستخدم ديناميكيًا (مثلاً require(pkgName) أو تُستخدم في سيرفر منفصل) — لا تَحذف إلا بعد التحقق اليدوي.

5. أرشفة الملفات/الأصول غير المستخدمة بدل الحذف الفوري
التقرير ذكر صورة واحدة غير مستخدمة؛ أنقلها إلى مجلد archive/ احتياطًا:



mkdir -p archive/unused-assets
git mv attached_assets/Screenshot_...Excel_1753789824406.jpg archive/unused-assets/ || mv attached_assets/Screenshot_...Excel_1753789824406.jpg archive/unused-assets/
git add .
git commit -m "chore: archive unused asset(s) identified by audit"

(غيّر اسم الملف كاملاً حسب المسار الفعلي الموجود لديك.)


---

ثانيًا — خطة عمل قصيرة المدى (خلال 2–7 أيام)

1. مراجعة الحزم المرشّحة للإزالة

اجمع الحزم التي اكتشفت أنها فعلاً غير مستخدمة في ملف audit-results/to-remove.txt.

أنشئ PR يحتوي على: package.json مع الحزم المحذوفة، تحديث package-lock.json/pnpm-lock.yaml، ونتائج بناء + اختبارات ناجحة.
إزالة آمنة:




# بعد التحقق اليدوي
npm uninstall <package1> <package2>  # أو pnpm remove ...
npm install  # (لتحديث الـ lockfile)
npx tsc --noEmit
npm test
git add package.json package-lock.json
git commit -m "chore: remove unused dependencies (audit)"
git push origin audit/cleanup-$(date +%F)
# ثم افتح PR واطلب مراجعة

2. معالجة ملفات orphans (madge)

افتح audit-results/madge-orphans.txt، ضع كل ملف في عمود: [مسار — هل اختبار؟ — هل قالب؟ — مذكور ديناميكياً؟].

للأهمّية: إن كانت ملفات مثل client/src/main.tsx أو صفحات أو مكونات شادكن موجودة — تجاهل إشارة madge كما ذكرت في التقرير.

للملفات التي فعلاً غير مستخدمة: git mv <file> archive/ ثم commit.



3. تحديث قاعدة بيانات المتصفحات (browserslist)



npx update-browserslist-db@latest
git add package.json package-lock.json
git commit -m "chore: update browserslist database"

4. تأكيد إعداد ESLint

شغّل ESLint وإصلح التحذيرات الحرجة. إذا الإعداد الآن جيد كما ورد — فقط التأكد:




npx eslint . --ext .js,.ts,.tsx --max-warnings=0

(هذا الأمر يجعل الفشل في CI عندما توجد تحذيرات مهمة.)


---

ثالثًا — خطة متوسطة/طويلة المدى (أسبوعان — شهر)

1. أضف CI تلقائي (GitHub Actions) لفحص الجودة
انسخ الملف التالي إلى .github/workflows/ci.yml ليشغّل الفحوص الأساسية على كل PR:



name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 3 1 * *' # مرة شهريا (UTC) لتشغيل التدقيق التلقائي

jobs:
  build-and-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install
        run: npm ci
      - name: TypeScript check
        run: npx tsc --noEmit
      - name: ESLint check
        run: npx eslint . --ext .js,.ts,.tsx
      - name: Run tests
        run: npm test || true
      - name: Run project audit script
        run: bash ./repo-audit.sh
      - name: Upload audit artifact
        uses: actions/upload-artifact@v4
        with:
          name: audit-results
          path: audit-results

> هذا يضمن أن كل PR اختباري ويمنع الدمج إن فشل tsc أو eslint.



2. أتمتة التدقيق الشهري

الـ workflow أعلاه يشغّل repo-audit.sh شهريًا ويحفظ النتائج كـ artifact — صحّي.



3. مراقبة الأداء والأخطاء بعد التنظيف

بعد إزالة حزم/أرشفة ملفات، راقب الـ logs وبيئة staging لمدة أسبوع قبل الدمج على main.





---

سكربت مفيد للمساعدة في اتخاذ قرار حذف الحزم (نسخة جاهزة)

احفظه كـ tools/check-deps-usage.sh ثم شغّله:

#!/usr/bin/env bash
set -e
mkdir -p audit-results
# الحزم المرشحة (تعديل حسب تقريرك)
pkgs=( @jridgewell/trace-mapping connect-pg-simple framer-motion memorystore next-themes openid-client passport passport-local react-icons tw-animate-css xlsx )

for pkg in "${pkgs[@]}"; do
  echo "=== $pkg ===" | tee -a audit-results/deps-usage.txt
  if rg -n "from ['\"]$pkg['\"]|require\(['\"]$pkg['\"]\)" > /dev/null 2>&1; then
    rg -n "from ['\"]$pkg['\"]|require\(['\"]$pkg['\"]\)" | tee -a audit-results/deps-usage.txt
  else
    echo "لا توجد إشارات استيراد مباشرة. افحص يدويًا (dynamic require أو مستخدم في سيرفر خارجي)." | tee -a audit-results/deps-usage.txt
  fi
  echo "" | tee -a audit-results/deps-usage.txt
done


---

قائمة فحص نهائية قبل عمل PR (Checklist للـ PR)

[ ] بناء المشروع ينجح (npx tsc --noEmit & npm run build).

[ ] كل الاختبارات تمر (أو وجود سبب واضح لعدم ذلك).

[ ] ESLint بلا أخطاء/تحذيرات حرجة.

[ ] أي حزمة محذوفة قد فُحصت واستُعرضت نتائج البحث (audit-results/deps-usage.txt).

[ ] أي ملف محذوف أو منقول إلى archive/ موصول بشرح في الـ PR (لماذا نقلت).

[ ] تم تشغيل الـ app محليًا (basic smoke test على صفحات رئيسية).

[ ] المراقبة بعد الدمج (staging) مفعّلة لمدة 48-72 ساعة.



---

خاتمة سريعة ونصيحة عملية (ومزحة صغيرة)

ابدأ الآن بالخطوات الفورية: شغّل npx tsc --noEmit ثم npx depcheck --json > audit-results/depcheck.json، وحاول أرشفة الصورة غير المستخدمة أولاً. بعد ذلك اتبع خطة إزالة الحزم بشكل مُحكَم عبر فرع وPR — لا تترك الحذف المباشر على main.

إذا تحب، أجهز لك الآن:

ملف .github/workflows/ci.yml كامل (نسخة مُخصّصة أكثر)، و/أو

git patch جاهز لنقل الأصول إلى archive/ وملف repo-audit.sh مُحسّن.


أخبرني أيٍ من الاثنين تريد — سأولّف لك الملفات مباشرة هنا جاهزة للصق. (ولا تقلق، ما راح أمسح شيء بنفسي — أقدّم لك الأوامر والملفات وتنفّذ أنت). 🚀

